to run the compiler make sure to iput these commands in the terminal, ignore thje warnings, as  long as you execture the following commands the parser will print the 
ir code and mips code

$ make clean

$ make parser

$ make parser

the compiler will run the test.cmm file which is the source code used for project 5 with compilition time of how long it takes to execute program in compiler
