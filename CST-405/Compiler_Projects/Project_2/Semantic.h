
// checks to make sure the functions in parser are correct like strings and varibles are correct


#include <sstream>
#pragma once
#include <string.h>
#include <string>
using namespace std;


string cBS(char* c){

    char* cstr = c;
    std::string str(cstr);
    return cstr;
    
}